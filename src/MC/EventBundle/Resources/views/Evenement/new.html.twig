{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Evenement creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('mycampus_event') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
